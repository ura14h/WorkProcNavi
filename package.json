{
  "name": "work-proc-navi",
  "version": "1.2.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "dist-electron/main.js",
  "productName": "WorkProcNavi",
  "scripts": {
    "dev": "concurrently -k \"tsup src/main.ts --format esm --platform node --external electron --out-dir dist-electron --watch\" \"tsup src/preload.ts --format cjs --platform node --external electron --out-dir dist-electron --watch\" \"vite\" \"wait-on tcp:5173 dist-electron/main.js dist-electron/preload.cjs && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build": "tsup src/main.ts --format esm --platform node --external electron --out-dir dist-electron && tsup src/preload.ts --format cjs --platform node --external electron --out-dir dist-electron && vite build",
    "clean": "node -e \"const fs = require('node:fs'); for (const dir of ['dist', 'dist-electron', 'release']) fs.rmSync(dir, { recursive: true, force: true });\"",
    "dist": "npm run dist:all",
    "dist:all": "npm run build && electron-builder --mac zip --arm64 --publish never && electron-builder --win portable --x64 --publish never && node scripts/package-windows-portable-zip.mjs",
    "dist:mac": "npm run build && electron-builder --mac zip --arm64 --publish never",
    "dist:win": "npm run dist:win:portable && node scripts/package-windows-portable-zip.mjs",
    "dist:win:portable": "npm run build && electron-builder --win portable --x64 --publish never",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "build": {
    "appId": "net.homeunix.hio.app.workprocnavi",
    "directories": {
      "output": "release",
      "buildResources": "resources"
    },
    "files": [
      "dist/**/*",
      "dist-electron/**/*",
      "package.json"
    ],
    "asar": true,
    "mac": {
      "icon": "resources/icon.png",
      "target": [
        {
          "target": "zip",
          "arch": [
            "arm64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-mac-${arch}.${ext}",
      "identity": null
    },
    "win": {
      "icon": "resources/icon.png",
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-win-portable-${arch}.${ext}",
      "signAndEditExecutable": false
    }
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "markdown-it": "^14.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sanitize-html": "^2.17.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3",
    "electron": "^37.0.0",
    "electron-builder": "^26.0.12",
    "tsup": "^8.5.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0",
    "wait-on": "^8.0.3"
  }
}
